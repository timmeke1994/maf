apiVersion: v1
kind: PersistentVolume
metadata:
  name: app-disk1
  namespace: python
spec:
  capacity:
  storage:
  accessModes:
    -ReadWriteMany
  cephfs:
  monitors:
    - "172.17.0.1:6789"
  users: admin
  secretRef:
    name: ceph-secret
  ReadOnly: false